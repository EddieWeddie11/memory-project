function Card() {
  return <div></div>
}

export default Card

// linking to the data base i.e colour
// what the card looks like
// needs an onClick event
// ifFlipped !ifFlipped
// ifMatched !ifMatched
