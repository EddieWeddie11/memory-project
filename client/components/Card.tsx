function Card () {
  return(
    <div>
      
    </div>
  )
}

default export Card


// linking to the data base i.e colour
// what the card looks like
// needs an onClick event 
// ifFlipped !ifFlipped
// ifMatched !ifMatched
